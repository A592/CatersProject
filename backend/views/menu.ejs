<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu for <%= restaurant.name %></title>
    <link rel="stylesheet" href="/css/menu.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body>
    <div class="container">
        <h1>Menu for <%= restaurant.name %></h1>
        <p><strong>Cuisine:</strong> <%= restaurant.cuisine %></p>
        <p><strong>Address:</strong> <%= restaurant.address %></p>
        <p><strong>Contact:</strong> <%= restaurant.contact %></p>

        <!-- Loop through the packages and display them -->
        <% if (packages.length === 0) { %>
            <p>No packages available for this restaurant.</p>
        <% } else { %>
            <% packages.forEach(package => { %>
                <div class="card">
                    <h2><%= package.name %></h2>
                    <img src="<%= package.image %>" alt="<%= package.name %> image" style="width: 200px; height: 150px;">
                    <p><%= package.description %></p>
                    <p><strong>Price:</strong> $<%= package.price %></p>
                    <input type="radio" id="<%= package.name %>" name="package" value="<%= package.name %>" data-price="<%= package.price %>">
                    <label for="<%= package.name %>">Select <%= package.name %></label>
                    <input type="hidden" id="restaurantId" value="<%= restaurant._id %>">
                </div>
            <% }) %>
        <% } %>

        <div class="billing">
            <label for="numPeople">Number of People:</label>
            <input type="number" id="numPeople" value="5" min="5" required>
            <p id="peopleError" style="color:red; display:none;">Please enter a valid number of people.</p>

            <p class="total-price">Total Price: <span id="totalPrice">$0</span></p>

            <button id="bookButton" disabled>Confirm Booking</button>
        </div>
    </div>

    <!-- Include the external JavaScript file from the public/js directory -->
   
    <script src="/js/menu.js"></script>
</body>
</html>
